# 0316 작업내역

## 📌 오늘 작업 요약
About 회사소개 페이지 대규모 리팩토링 + 맞춤추천 폼 Supabase 연동 + 어드민 추천관리 페이지

---

## 1. About 페이지 AI 섹션 리팩토링

### AI 스텝 카드 그리드 (3x2)
- **수정 파일**: `app/about/AboutClient.tsx`, `app/about/about.module.css`
- **변경 전**: 세로 나열 1~6번 스텝 (화살표 구분, 왼쪽 정렬)
- **변경 후**: 3x2 카드 그리드 배치
  - 각 카드: 번호 뱃지(네이비 배경) + 제목(볼드) + 설명(서브 텍스트)
  - 호버 효과: `translateY(-4px)` + `box-shadow`
  - 모바일: 2x3 그리드로 전환
- **CSS 클래스**: `.aiStepGrid`, `.aiStepCard`, `.aiStepCardNum`, `.aiStepCardTitle`, `.aiStepCardDesc`

### 카드 숫자 중앙정렬
- **문제**: 번호 뱃지(01~06)가 왼쪽 정렬
- **수정**: `text-align: center` + `margin: 0 auto 16px` 추가

---

## 2. 챗봇 팝업 연결 (About 페이지)

### 문제
- About 페이지에 `ChatFloatingButton` 컴포넌트가 없어서 "AI 상담 시작하기" 버튼이 작동 안 함
- `ChatFloatingButton`은 `BottomNav`에만 포함되어 있었음

### 해결
- **수정 파일**: `app/about/AboutClient.tsx`, `components/chatbot/ChatFloatingButton.tsx`
- `ChatFloatingButton`에 `open-chatbot` 커스텀 이벤트 리스너 추가:
  ```tsx
  useEffect(() => {
      const handleOpenChat = () => setIsOpen(true);
      window.addEventListener('open-chatbot', handleOpenChat);
      return () => window.removeEventListener('open-chatbot', handleOpenChat);
  }, []);
  ```
- About 페이지에 `ChatFloatingButton` import + Fragment 래핑:
  ```tsx
  return (
      <>
          <div className={s.page}>...</div>
          <ChatFloatingButton />
      </>
  );
  ```
- "AI 상담 시작하기" 버튼: `Link` → `button` + `window.dispatchEvent(new Event('open-chatbot'))`

---

## 3. 서비스 범위 섹션 풍성화

### 변경 전
- 단순 체크마크/X 목록 (각 항목 제목만)
- 왼쪽 `[` 브래킷 border 스타일

### 변경 후 (2차 피드백 반영)
- **수정 파일**: `app/about/AboutClient.tsx`, `app/about/about.module.css`
- 각 항목에 **제목 + 설명 텍스트** 2줄 구조
- 카드 헤더에 **타이틀 + 하단 구분선** (`padding-bottom: 20px; border-bottom`)
- **상단 컬러 바** 디자인: `border-top: 4px solid var(--about-navy)` (Yes), `border-top: 4px solid #ddd` (No)
- ✓ 아이콘: **파란색** (`#2563eb`, 18px)
- ✕ 아이콘: **빨간색** (`#ef4444`, 18px)
- 카드 라운딩: `border-radius: 16px`
- 호버 효과: `translateY(-4px)` + `box-shadow`
- 모바일: 1열 전환

---

## 4. 서비스 현황 숫자 동적화

### 변경 전
- 하드코딩: `2026년 3월`, `127건`, `48명`, `3일 전`

### 변경 후
- **수정 파일**: `app/about/AboutClient.tsx`
- 현재 연/월 자동 표시: `new Date()` → `{year}년 {month}월`
- 상담 건수: 시드 기반 월별 변화 (90~180건 범위)
  ```ts
  const seed = year * 12 + month;
  const consultCount = 90 + (seed * 7 + 13) % 91;
  ```
- 오늘 조회수: 날짜별 변화 (30~80명 범위)
- 가격 업데이트: 매월 1일, 15일 기준 `N일 전` 계산

### 폰트 크기 +2px (피드백 반영)
- `.urgencyTitle`: 14px → **16px**
- `.urgencyStat`: 13px → **15px**

---

## 5. 맞춤추천 폼 → Supabase 연동

### Supabase 테이블 생성
- **프로젝트**: `jbydmhfuqnpukfutvrgs`
- **테이블**: `recommendation_requests`
- **컬럼**: `id (BIGSERIAL PK)`, `region`, `facility_type`, `budget`, `phone`, `message`, `status (default: pending)`, `created_at`
- **RLS**: 삽입은 누구나(폼 제출), 읽기는 인증된 사용자만(어드민)

### API Route
- **파일**: `app/api/recommendation/route.ts`
- **기능**:
  - 필수값 검증 (region, facilityType, phone)
  - 전화번호 형식 검증 (10~11자리)
  - Supabase INSERT
  - Slack 알림 (SLACK_WEBHOOK_URL 설정 시)

### 폼 UI 개선
- **수정 파일**: `app/about/AboutClient.tsx`
- 상태 관리: `formRegion`, `formType`, `formBudget`, `formPhone`, `formMessage`, `formSubmitting`, `formSubmitted`, `formError`
- 제출 핸들러: `handleFormSubmit()` — fetch → /api/recommendation
- 에러 표시: 빨간 텍스트 메시지
- 로딩 상태: 버튼 "접수 중..." + opacity 0.6
- 성공 화면: ✅ "접수 완료!" + "3시간 내 결과 전달" 메시지

### 폼 레이아웃 수정 (피드백 반영)
- `.formCard`: `border-radius: 0` → **16px**, `padding: 40px 32px`
- `.formRow`: `gap: 12px` → **20px**, `margin-bottom: 20px` 추가

---

## 6. 어드민 맞춤추천 관리 페이지

### 어드민 API
- **파일**: `app/api/admin/recommendations/route.ts`
- **GET**: 전체 목록 (최신순)
- **PATCH**: 상태 변경 (pending → contacted → completed)
- **DELETE**: 삭제

### 어드민 페이지
- **파일**: `app/admin/recommendations/page.tsx`
- **기능**:
  - PC: 테이블 뷰 (연락처, 지역, 시설유형, 예산, 신청일, 상태, 관리)
  - 모바일: 카드 뷰
  - 검색: 연락처, 지역, 시설유형
  - 상세 모달: 모든 정보 + 상태 변경(대기중/연락완료/추천완료) + 삭제
  - 전화번호 포맷: `01012345678` → `010-1234-5678`

### 사이드바 메뉴 추가
- **수정 파일**: `app/admin/layout.tsx`
- `Gift` 아이콘 import 추가
- navItems에 `{ label: '맞춤추천관리', icon: IconGift, link: '/admin/recommendations' }` 추가

---

## 7. 카카오톡 채널 → 장지 추천 스크롤 버튼

- **수정 파일**: `app/about/AboutClient.tsx`, `app/about/about.module.css`
- 변경 전: "시설 정보 제보 / 카카오톡 채널" 텍스트
- 변경 후: "무료 장지 추천 받기 / 1:1 맞춤 상담 신청 →" 버튼
- 클릭 시: `document.querySelector('#recommendation-form')?.scrollIntoView({ behavior: 'smooth' })`

---

## 8. guarantee 카드 중앙정렬

- **수정 파일**: `app/about/about.module.css`
- `.guaranteeCard`: `text-align: center` 추가
- `.guaranteeCardIcon`: `margin: 0 auto` 추가
- `.guaranteeCardLabel`: `margin: 0 auto` 추가

---

## 📝 커밋 이력
| 커밋 | 메시지 |
|---|---|
| `c5d89d5` | feat: About 페이지 리팩토링 + 맞춤추천 폼 Supabase 연동 + 어드민 추천관리 |

---

## 📁 변경 파일 목록
| 파일 | 상태 | 설명 |
|---|---|---|
| `app/about/AboutClient.tsx` | 신규 | About 페이지 클라이언트 컴포넌트 (전면 재작성) |
| `app/about/about.module.css` | 신규 | About 페이지 전용 CSS (2,100라인+) |
| `app/about/page.tsx` | 수정 | 메타데이터 + AboutClient 렌더링 |
| `app/api/recommendation/route.ts` | 신규 | 맞춤추천 폼 제출 API |
| `app/api/admin/recommendations/route.ts` | 신규 | 어드민 추천관리 API (GET/PATCH/DELETE) |
| `app/admin/recommendations/page.tsx` | 신규 | 어드민 맞춤추천관리 페이지 |
| `app/admin/layout.tsx` | 수정 | 사이드바에 맞춤추천관리 메뉴 추가 |
| `components/chatbot/ChatFloatingButton.tsx` | 수정 | 외부 open-chatbot 이벤트 리스너 추가 |

---

## 9. 빌드 에러 수정

### tesseract.js 타입 에러
- **파일**: `lib/pdfParser.ts`
- **문제**: `tesseract.js`, `pdfjs-dist` 모듈의 타입 선언 파일이 없어 빌드 실패
- **해결**: `// @ts-ignore` 주석 추가 (import 및 dynamic import 앞)

### Supabase 클라이언트 초기화 에러
- **파일**: `app/api/recommendation/route.ts`, `app/api/admin/recommendations/route.ts`
- **문제**: 모듈 레벨에서 `createClient()` 호출 시 환경변수 없으면 에러
- **해결**: Lazy initialization 패턴 적용
  ```ts
  let supabaseInstance: any = null;
  function getSupabase() {
      if (!supabaseInstance) {
          supabaseInstance = createClient(
              process.env.NEXT_PUBLIC_SUPABASE_URL!,
              process.env.SUPABASE_SERVICE_ROLE_KEY!
          );
      }
      return supabaseInstance;
  }
  ```

---

## 10. About 페이지 SEO 강화 (감사 후 수정)

### Sequential Thinking MCP 5단계 분석
- About 페이지 전체 1,229줄 코드 분석
- SEO/콘텐츠/기술적 문제 총 18건 식별
- 심각 5건, 중간 7건, 경미 6건으로 분류

### 수정 내역

#### JSON-LD 구조화 데이터 추가
- **파일**: `app/about/page.tsx`
- `Organization` 스키마: 회사명, URL, 로고, 설명, 연락처
- `WebPage` 스키마: 페이지명, URL, 설명, BreadcrumbList(홈→회사소개)
- `<script type="application/ld+json">` 태그로 서버 컴포넌트에서 렌더링

#### OG Image + Twitter Card 추가
- **파일**: `app/about/page.tsx`, `public/og-image.png` (신규)
- `og:image`: 1200x630 네이비 배경 브랜드 이미지 생성 + 메타 추가
- `og:url`: `https://daedaesonson.com/about`
- `twitter:card`: `summary_large_image`
- `twitter:image`: og-image.png 연결

#### canonical URL 추가
- **파일**: `app/about/page.tsx`
- `alternates: { canonical: 'https://daedaesonson.com/about' }`

#### 법적 표현 수정
- **meta title**: `전국 장지 조회 1등 플랫폼` → `전국 장지 가격 비교 전문 플랫폼`
- **본문**: `장례 업계 최초, 전국 장지 가격을` → `전국 장지 가격을`
- 근거 없는 "1등", "최초" 표현 → 공정거래법 위반 가능성 제거

#### 영어 태그 한글화 (12곳)
| 영어 | 한글 |
|---|---|
| DATA SECTION 01~04 | 데이터 분석 01~04 |
| INTERACTIVE CALCULATOR | 장지 비용 계산기 |
| Interactive Example | 실제 비교 예시 |
| TRANSPARENCY MOVEMENT | 가격 투명화 운동 |
| SPECIAL OFFER | 특별 혜택 |
| HOW TO PARTICIPATE | 참여 방법 |
| DAEDAESONSON GUARANTEE | 대대손손 보증 |
| PREMIUM PREVIEW | 시설 미리보기 |

#### © 연도 동적화
- `© 2025 대대손손` → `© {new Date().getFullYear()} 대대손손`

---

## 11. 광고 전 종합 점검 (Sequential Thinking 3단계)

### ✅ 정상 확인 항목
| 항목 | 상태 | 비고 |
|---|---|---|
| sitemap.xml 존재 | ✅ | 홈, /list, 지역별 페이지 포함 |
| robots.txt | ✅ | `/admin/`, `/api/` 차단, sitemap 경로 명시 |
| GA4 태그 | ✅ | `G-XHCFVSDRDY` 설치 (layout.tsx) |
| Google Ads 전환 추적 | ✅ | gtag 이벤트 (홈 조회, 검색, 리스트 조회) |
| 이용약관 | ✅ | `/terms` 페이지 존재 |
| 개인정보처리방침 | ✅ | `/privacy` 페이지 존재 (법률 준수) |
| naver-site-verification | ✅ | 네이버 서치어드바이저 등록됨 |
| google-site-verification | ✅ | Google Search Console 등록됨 |
| HTTPS | ✅ | Vercel 자동 SSL |

### ⚠️ 발견된 문제
| 항목 | 상태 | 비고 |
|---|---|---|
| sitemap.xml에 /about 페이지 | ❌ 누락 | 검색엔진이 About 페이지를 덜 크롤링 |
| About 폼에 개인정보 동의 체크박스 | ❌ 없음 | partnership 폼에는 있지만 About 맞춤추천 폼에는 없음 |

---

## 📝 커밋 이력
| 커밋 | 메시지 |
|---|---|
| `c5d89d5` | feat: About 페이지 리팩토링 + 맞춤추천 폼 Supabase 연동 + 어드민 추천관리 |
| `622ec0d` | fix: 빌드 에러 수정 (ts-ignore + Supabase lazy init) |
| `7a3a2d6` | seo: About 페이지 SEO 강화 — JSON-LD, OG Image, 영어태그 한글화, 법적표현 수정 |

---

## 📁 추가 변경 파일
| 파일 | 상태 | 설명 |
|---|---|---|
| `app/about/page.tsx` | 수정 | JSON-LD + OG Image + canonical + 법적표현 |
| `app/about/AboutClient.tsx` | 수정 | 영어태그 한글화 12곳 + ©연도 동적화 + "업계 최초" 삭제 |
| `public/og-image.png` | 신규 | OG Image (1200x630 네이비 브랜드 이미지) |
| `lib/pdfParser.ts` | 수정 | @ts-ignore 추가 (tesseract.js, pdfjs-dist) |
| `app/api/recommendation/route.ts` | 수정 | Supabase lazy initialization |
| `app/api/admin/recommendations/route.ts` | 수정 | Supabase lazy initialization |
