# 📋 2026-03-10 작업 내역

## 🎯 작업 목표
**PC/모바일 필터 UI 통일** — Popover 방식 폐기 → Drawer 방식으로 일괄 전환, 문의시설 제외 기능 전면 확대

---

## 📌 작업 항목

### 1. 모바일 필터 Drawer 닫히는 버그 수정
- **문제**: 모바일에서 필터 버튼 클릭 시 Drawer가 열렸다가 즉시 닫힘
- **원인**: Mantine v8 Popover controlled mode에서 `opened` + `onChange` + `onClick`이 동시 사용 시 double-toggle 충돌
- **해결**: 모바일 Drawer 동작 안정화

### 2. PC 필터 Popover → Drawer 전환 (메인 페이지)
- **문제**: PC에서 "전체 시설" 클릭 시 Popover 드롭다운이 열리지 않음
- **원인 1**: Mantine v8 Popover z-index 기본값(300)이 사이드바 z-index(1000)보다 낮아서 뒤에 숨김
- **원인 2**: Drawer 컴포넌트가 `{isMobile && mobileView === 'map' && (...)}` 조건 안에 감싸져 있어 PC에서 렌더링되지 않음
- **해결**:
  - Popover 제거 → Drawer 방식으로 전환
  - Drawer를 모바일 조건 밖으로 이동 (PC+모바일 공용)
  - PC: `position="left"`, `size={400}` (사이드바 크기)
  - 모바일: `position="bottom"`, `size="100%"` (풀스크린)

### 3. PC 필터 Popover → Drawer 전환 (리스트 페이지)
- **문제**: 리스트 페이지도 동일한 Popover 문제
- **해결**:
  - PC/모바일 분기(`{!isMobile ? <Popover> : <button>}`) 제거
  - 하나의 버튼 + Drawer로 통일
  - `{isMobile && (<Drawer>)}` 조건 제거 → 항상 렌더링
  - PC: `position="left"`, `size={400}`
  - 모바일: `position="bottom"`, `size="100%"`

### 4. PC "주변 시설 보기" 오버레이 필터 개선
- **구조**: `HomeClient.tsx` 줄 1277~ — 400px fixed 패널 (z-index: 2000)
- **문제**: Popover z-index(300) < 패널 z-index(2000)로 드롭다운 안 보임 + "문의제외" 버튼 없음
- **해결**:
  - Popover 제거 → Drawer 방식 전환 (`position="left"`, `size={400}`)
  - 독립 state 추가: `nearbyDrawerOpen`, `tempNearbyCategory`, `nearbyHideInquiry`
  - "검색 결과 N개" 옆에 "문의시설 제외" 체크박스 추가
  - 필터링 로직에 `nearbyHideInquiry` 반영: `(f.priceRange?.min ?? 0) > 0`

### 6. 어드민 대시보드 실제 DB 연동
- **문제**: 4개 통계 카드 중 3개가 하드코딩 (`'42,910'`, `'1,290'`, `'3'`), 증감률도 전부 하드코딩
- **해결**: `Promise.all`로 7개 API 병렬 호출 → 모든 카드 실제 데이터 연동
- **연동 항목**:

| 카드 | 변경 전 | 변경 후 | API |
|------|---------|--------|-----|
| 총 등록 시설 | ✅ 연동 | ✅ 유지 | `/api/facilities` |
| 이번 달 조회수 | ❌ `'42,910'` | 🗑️ 제거 | - |
| 활성 사용자 | ❌ `'1,290'` | → **가입 회원** | `/api/admin/members` |
| 신고/수정 요청 | ❌ `'3'` | → **수정 요청** | `/api/corrections` |
| 상담 신청 | 없음 | ✅ **신규** | `/api/consult` |
| 댓글 문의 | 없음 | ✅ **신규** | `/api/admin/inquiries/count` |
| 방문 후기 | 없음 | ✅ **신규** | `/api/admin/reviews` |
| 제휴 문의 | 없음 | ✅ **신규** | `/api/partnership` |

- **성능**: `Promise.all` 병렬 호출 → 가장 느린 API(facilities ~1-2초) 하나만 대기
- **적용 파일**: `app/admin/page.tsx`

---

## 📁 변경된 파일 목록

| 파일 | 변경 유형 | 설명 |
|------|---------|------|
| `app/HomeClient.tsx` | 수정 | PC 필터 Popover→Drawer 전환, Drawer를 모바일 조건 밖으로 이동, 주변시설 오버레이 Drawer 추가, 문의시설 제외 기능 |
| `app/list/page.tsx` | 수정 | PC/모바일 필터 분기 제거 → Drawer 통일, 문의제외 버튼 추가 |
| `components/list/FacilityList.tsx` | 수정 | "검색 결과 N개" 옆 "문의시설 제외" 체크박스 추가, props 확장 |
| `components/list/FilterBar.tsx` | 수정 | `hideInquiry` prop 추가 (호환성) |
| `app/admin/page.tsx` | 수정 | 대시보드 하드코딩 제거, 7개 API 병렬 fetch 연동 |

---

## 🔧 필터 UI 구조 (최종)

### PC 메인 페이지
```
┌─ 사이드바 (400px) ───────────────────────┐
│ [전체 시설 ▾]  ← 클릭 → Drawer (left, 400px)   │
│ ─────────────────────────────────────────│
│ 서울특별시 관악구        [전체|공설|사설]     │
│ 검색 결과 1455개      □ 문의시설 제외      │
│ ┌─────────────────────────────────────┐  │
│ │ 시설 카드 리스트...                     │  │
└─┴─────────────────────────────────────┴──┘
```

### PC 주변 시설 보기 오버레이
```
┌─ 오버레이 (400px, z:2000) ───────────────┐
│ < 서울특별시 중구 주변 시설 보기             │
│ [전체 시설 ▾]  ← 클릭 → Drawer (left, 400px) │
│ 검색 결과 176개       □ 문의시설 제외      │
│ ┌─────────────────────────────────────┐  │
│ │ 시설 카드 리스트...                     │  │
└─┴─────────────────────────────────────┴──┘
```

### 모바일
```
[전체 시설 ▾] [문의제외]
       ↓ 클릭
┌─ Drawer (bottom, 100%) ──────┐
│ ✕             필터             │
│ 시설 유형                      │
│ ☑ 봉안당  ☑ 수목장             │
│ ☑ 공원묘지  ☑ 화장장           │
│ ──────────                    │
│ □ 문의 가격 시설 제외            │
│                                │
│ [↺ 선택해제]  [적용하기]         │
└────────────────────────────────┘
```

---

## 📊 커밋 로그

```
72c448e fix: PC 필터 Popover→Drawer 통일, 주변시설 오버레이 문의제외 추가
933ddf7 fix: PC 메인 문의시설 제외 체크박스 검색결과 옆 이동, 필터바 정리
976a0ce feat: 어드민 대시보드 실제 DB 연동 - 하드코딩 제거, 7개 API 병렬 fetch
```

---

## 💡 발견된 핵심 이슈

### Mantine v8 Popover z-index 문제
- Popover.Dropdown의 기본 z-index: **300**
- PC 사이드바 z-index: **1000**
- 주변시설 오버레이 z-index: **2000**
- → Popover가 항상 뒤에 숨어서 보이지 않음
- **해결**: Popover 폐기 → Drawer로 전환 (z-index: 10000)

### Drawer 렌더링 조건 문제
- Drawer가 `{isMobile && mobileView === 'map' && (...)}` 안에 있어서 PC에서 렌더되지 않음
- 버튼은 `drawerFilterOpen=true` 설정하지만, Drawer 자체가 DOM에 없으므로 아무 반응 없음
- **해결**: Drawer를 조건 밖으로 이동

