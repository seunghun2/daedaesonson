# 0314 작업내역

## 📌 오늘 작업 요약
챗봇 Phase 3 완료 (시설 비교 카드, 가격표 인라인, 주변 시설 추천)

---

## 1. 시설 비교 카드 — 카드 표시 조건 개선
- **문제**: 일반 대화에서도 카드가 매번 나옴 / 상세페이지에서 추천 불가
- **수정 파일**: `app/api/chat/route.ts`
- **수정 내용**:
  - 추천 트리거(`추천/비교/어디/어떤` 등) 있을 때만 카드 표시
  - 상세페이지(`facilityContext`)에서도 명시적 추천/비교/비슷한 요청 시 카드 허용
  - 현재 보고 있는 시설 자동 제외 (`excludeId`)
  - "다른 곳" 요청 시 결과 3개 오프셋 (이전 추천 스킵)
  - `wantsPublic/wantsPrivate` 구분 시 해당 유형만, 미지정 시 공립1+민간2 혼합

## 2. 가격표 인라인 렌더링
- **수정 파일**: `app/api/chat/route.ts`, `components/chatbot/AIChatbot.tsx`
- **Backend**:
  - 가격 키워드(`가격/얼마/비용/요금` 등) 감지 → `pricingTable` JSON 생성
  - `standardizedPrices`를 `groupType`별 요약 (범위 or 개별 가격)
  - 관리비 별도 분리, 시설명/공립·민간 포함
- **Frontend**:
  - `PricingTable/PricingSection/PricingRow` 인터페이스 정의
  - NAVY 헤더 + 섹션별 가격행 + 관리비 + "자세히 보기" 버튼 카드 UI
- **버그 수정**: `FacilityDetail.tsx`에서 `id: Number(facility.id)` → `id: facility.id`
  - `Number("park-0006")` = `NaN` 되어 시설 못 찾던 버그 해결

## 3. 카드 가격 표시 개선
- **문제**: 추천 카드에 가격이 안 나옴 (standardizedPrices 없는 시설)
- **수정 파일**: `app/api/chat/route.ts`
- **수정 내용**: `minPrice` fallback 체인: `standardizedPrices` → `representativePrice` → `priceRange.min`

## 4. 주변 시설 추천 (좌표 기반)
- **수정 파일**: `app/api/chat/route.ts`, `components/chatbot/AIChatbot.tsx`
- **Backend**:
  - `haversineKm()` 함수 추가 (위도/경도 → km 거리 계산)
  - 키워드 감지: `주변/근처/가까운/근방/인근/가까이`
  - 상세페이지에서 현재 시설 좌표 기준 **반경 50km** 이내 시설 검색
  - 거리순 정렬, 가장 가까운 3개 선택
  - 카드에 `distanceKm` 필드 추가
- **Frontend**:
  - `FacilityCard` 인터페이스에 `distanceKm?` 추가
  - 카드 주소 옆에 네이비 뱃지로 거리 표시 (예: `0.8km`)

---

## 📊 Phase 3 완료 현황
| 기능 | 상태 |
|------|------|
| 시설 비교 카드 (조건부 표시 + 공립/민간 혼합) | ✅ |
| 가격표 인라인 렌더링 | ✅ |
| 가격 필터 상담 (500만원대 봉안당 등) | ✅ (0313 완료) |
| 주변 시설 추천 (Haversine 좌표 기반) | ✅ |

---

## 🔜 다음 작업 (Phase 4 CRM)
- [ ] 어드민 AI 상담 탭 재설계 — 대화 전체 열람 + 상담 메모
- [ ] 태그 분류 (급함, VIP, 재상담 등)
- [ ] 고객 프로필 (반복 상담 추적)
- [ ] 상담 통계 대시보드

---

## 5. 챗봇 디버깅 — 고급 MCP 분석 + 21건 수정

> 정적 코드 분석 1,800줄+ (route.ts 1,010줄 + AIChatbot.tsx 879줄)
> Sequential Thinking MCP로 3차 검증. grep 전수 조사까지.

### 🔴 심각 버그 (8건)

| # | 버그 | 수정 | 파일 |
|---|------|------|------|
| 1 | JSON 매 요청마다 fs.readFile | `getFacilities()` 모듈 레벨 캐싱 | route.ts |
| 2 | 카테고리/가격 검색 시 AI 응답 오염 | `allText` → `userText` (사용자만) | route.ts |
| 3 | 스트리밍 중 레이스 컨디션 | `streamingText !== null` 체크 + 기존 인터벌 clear | AIChatbot.tsx |
| 4 | 시설 전환 시 sessionId 미초기화 | `setSessionId(null)` + sessionStorage 삭제 | AIChatbot.tsx |
| 5 | URL regex `g` 플래그 → lastIndex 버그 | g 제거 + `startsWith` 판별 | AIChatbot.tsx |
| 6 | "다른 곳" 요청 시 동일 시설 반복 | AI 응답에서 추천 시설 ID 파싱 → Set으로 제외 | route.ts |
| 7 | "다른 곳" 요청 시 AI가 가이드셀링 재시작 | facilityData에 `[중요 맥락]` 주입 | route.ts |
| 8 | 프롬프트 인젝션 취약점 | `facilityContext` → `verifiedContext` 서버 검증 | route.ts |

### 🟠 중간 버그 (6건)

| # | 버그 | 수정 | 파일 |
|---|------|------|------|
| 9 | 스트리밍 인터벌 cleanup 누락 | useEffect return cleanup | AIChatbot.tsx |
| 10 | 퀵 리플라이 더블 클릭 | `isLoading \|\| streamingText` 가드 | AIChatbot.tsx |
| 11 | history 전달량 불일치 (-10 vs -20) | `.slice(-20)` 통일 (메인 + 퀵 리플라이) | AIChatbot.tsx |
| 12 | sessionStorage 용량 초과 위험 | cards/table 제외 lite 저장 + try-catch | AIChatbot.tsx |
| 13 | 가격 범위 파싱 미지원 | 한글숫자 + "200~500만원" rangeMatch 추가 | route.ts |
| 17 | Supabase 에러 로깅 누락 | `console.error` 추가 | route.ts |

### 🔍 검증 단계 추가 수정 (7건)

| 이슈 | 수정 |
|------|------|
| `facilityContext` 직접 참조 6곳 잔존 | 카드/주변시설/greeting/세션저장/shouldShowCards/pricingTarget → `verifiedContext` |
| `rangeMatch` 선언만 하고 미사용 | 가격 필터에 범위 우선 적용 |

### 검증 결과
- TypeScript 빌드 ✅ (chat 관련 에러 0)
- route.ts `facilityContext` grep: 6건 → 모두 검증 로직/주석 (데이터 활용 0)
- 퀵 리플라이 history `.slice(-20)` 통일 ✅

### 미수정 (🟡 경미 — 추후 진행)
- #14 시설명 검색 가격필터 우회
- #15 메인 "근처" 키워드 Geolocation 필요
- #16 관리비 feeType 누락
- #18 이미지 첨부 미완성
- #19 초기 퀵 리플라이 자동전송
- #20 base64 메모리 누수
- #21 z-index 충돌
- #22 비교 카드 크로스 카테고리
