# 📋 2026-03-13 작업내역

## 🎯 작업 목표
**AI 챗봇 UI/UX 전면 개선** — FAB 롤링 텍스트, 대손이 캐릭터 아이콘, 상세보기 버튼 위계 조정, 프롬프트 강화, 마커 깃발 색상 개선

---

## 📌 작업 항목

### 1. FAB 말풍선 롤링 텍스트 구현
- **기존**: 고정 문구 1개 ("대손AI 상담사 무엇을 도와드릴까요?")
- **변경**: 3개 CTA 문구가 3초 간격으로 페이드인/아웃 롤링
  - "대손AI 상담사 · 무엇을 도와드릴까요?"
  - "장지 비교·추천 · 맞춤 상담 도와드려요"
  - "24시간 무료 상담 · 편하게 물어보세요"
- 전체 1사이클(9초) + 여유시간 후 13초에 자동 숨김
- CX/UX 접근: 정서 공감 → 기능 설명 → 실용성 순서 배치

### 2. FAB 지도 탭 시 슬라이드 아웃
- 지도 터치 시 `uiHidden` 상태 활성화 → FAB가 `translateX(100px)`으로 오른쪽 밖으로 슬라이드
- `BottomNav` → `ChatFloatingButton`에 `hidden` prop 전달
- hover 이벤트와 hidden 상태 충돌 방지 처리

### 3. 대손이 캐릭터 아이콘 적용
- 유저 제공 `대손이아이콘.svg`를 `public/images/daesoni-icon.svg`로 배치
- 적용 위치 2군데:
  - 챗봇 헤더 (40x40, 아이콘 34px)
  - 챗봇 인트로 (56x56, 아이콘 46px)
- 배경색: 네이비(`#302E92`) → 연한 라벤더(`#EEEDFA`)로 변경하여 아이콘 가시성 확보

### 4. 상세보기 버튼 위계 조정
- **기존**: 진한 네이비 filled 버튼 (답변 텍스트보다 눈에 띔)
- **변경**: 아웃라인 스타일 (`transparent` 배경 + 네이비 `1px` 테두리)
- 텍스트: "상세 보기" → "상세 보기 →"
- fontSize 13→12, padding 줄여서 시각적 위계 톤다운

### 5. AI 프롬프트 전면 강화
- **어미 다양화**: "~입니다" 연속 2번 금지 → ~해요, ~거든요, ~추천드려요, ~좋습니다 혼합
- **추천 이유 필수**: 시설마다 "왜 이 시설이 좋은지" 1줄 덧붙임
- **숫자 비교 강제**: "저렴합니다" → "같은 지역 평균보다 30% 저렴해요"
- **후속 질문 의무화**: 매 답변 마지막에 상담 이어가기 질문
- **응답 구조 개선**: 핵심 진단 → 비교 추천 → 대손이 팁 → 후속 질문

### 6. 시설보기 버튼 아이콘 분기
- PC: ≡ 햄버거 SVG 아이콘 유지
- 모바일: 아이콘 없이 텍스트만 표시

### 7. 마커 깃발 색상 개선
- 마커 V자 깃발: 순백색(`#efefef`) → 연회색(`#d5d5d5`)
- 지도 배경과 구분되도록 시인성 개선

---

## 📁 변경된 파일 목록

| 파일 | 변경 유형 | 설명 |
|------|---------|------|
| `components/chatbot/ChatFloatingButton.tsx` | 전면 재작성 | 롤링 텍스트 3개 문구, 페이드 애니메이션, hidden 슬라이드 아웃 |
| `components/chatbot/AIChatbot.tsx` | 수정 | 대손이 아이콘 적용(2군데), 상세보기 버튼 아웃라인 스타일 |
| `components/common/BottomNav.tsx` | 수정 | `hidden` prop을 `ChatFloatingButton`에 전달 |
| `components/map/NaverMap.tsx` | 수정 | PC 시설보기 아이콘 분기, 마커 깃발 연회색 |
| `components/detail/FacilityDetail.tsx` | 수정 | 말풍선 `width: fit-content` 적용 |
| `app/globals.css` | 수정 | `labelExpand`/`labelCollapse` `max-width` 400px |
| `app/api/chat/route.ts` | 수정 | 프롬프트 톤&매너, 응답 구조 전면 강화 |
| `public/images/daesoni-icon.svg` | 신규 | 대손이 캐릭터 아이콘 |

---

## 📊 커밋 로그

```
ba55e75 feat: 챗봇 UI 개선 - 대손이 아이콘 적용, 롤링 텍스트, 버튼 톤다운, 프롬프트 강화, 마커 깃발 연회색
```

---

## 💡 주요 설계 결정

### 롤링 텍스트 CX/UX 접근
- 3개 문구를 정서 공감 → 기능 설명 → 실용성 순서로 배치
- 3초 간격은 사용자가 읽고 인지하기 충분한 시간
- `sessionStorage`로 세션 당 1회만 노출 → 반복 피로 방지

### 상세보기 버튼 위계
- 챗봇 답변에서 "정보 텍스트 > CTA 버튼" 위계가 올바름
- filled 버튼은 답변보다 시선을 뺏어가므로 아웃라인으로 톤다운

### 프롬프트 어미 다양화
- "~입니다"가 반복되면 기계적·딱딱한 느낌
- "~해요", "~거든요" 등 혼합하여 친구 같은 전문가 톤 구현
